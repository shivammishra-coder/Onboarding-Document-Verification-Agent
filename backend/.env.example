# Server
PORT=5000

# JWT
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN_HOURS=8

# Optional: plug in a real LLM for AI cross-validation (see app/pipeline/stage5_ai_cross_validation.py)
# ANTHROPIC_API_KEY=
